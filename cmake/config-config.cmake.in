get_filename_component(CONFIG_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET @PROJECT_NAME@::@PROJECT_NAME@)
    include("${CONFIG_CMAKE_DIR}/@PROJECT_NAME@-targets.cmake")
endif()

set(CONFIG_LIBRARIES @PROJECT_NAME@::@PROJECT_NAME@)
